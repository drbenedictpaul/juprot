#!/bin/sh

# Set the environment variable to ensure the correct Python is used
export JULIA_PYTHONCALL_EXE="@CondaPkg"

# Use exec to replace the shell process with the Julia process.
# We call bootstrap.jl directly without any extra Genie flags.
exec julia --project=. bootstrap.jl
